# Copy to .env and fill in your values. .env is gitignored — never commit it.
#
# GitHub personal access token (classic or fine-grained) with "repo" scope.
# Create: GitHub → Settings → Developer settings → Personal access tokens
GITHUB_TOKEN=

# Optional (defaults shown)
GITHUB_USERNAME=haroun97
GITHUB_REPO=Noahlabs
