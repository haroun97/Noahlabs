# Environment / secrets
.env
.env.local

# Local dev tooling (not part of the challenge)
scripts/push-github.sh

# Internal planning / review notes (not part of the challenge brief)
document/

# Dependencies (reinstall with npm install / pip / Docker)
node_modules/
.venv/
venv/

# OS / editor
.DS_Store
.idea/
.vscode/

# Cursor IDE (local rules — not part of the challenge)
.cursor/
